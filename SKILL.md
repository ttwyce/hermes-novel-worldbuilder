---
name: novel-worldbuilder
description: 根据用户输入的剧情梗概，自动生成完整详实的小说设定集。自动为每本书创建独立目录，存放设定、人物、大纲、正文等所有文件。支持剧情推进时动态追加角色、更新关系。
---
# novel-worldbuilder

## 快速入口

章节写入流程请见：`references/chapter-writing-workflow.md`（含审核报告清理、批量更新技巧、常见错误表）

**⚠️ 必读**：常见错误与事故见 `references/common-pitfalls.md`（截断标记灾难/字数验证/追踪文件更新/子代理超时等教训）

## 职能

本技能职能为：接收用户剧情梗概 → 生成书名选项 → 确认后初始化项目 → 逐文件生成设定集 → 写正文时按流程执行追踪文件更新。

**核心原则**：主 Agent 逐文件生成，禁止子代理批量生成。章节文件只写正文，审核报告不保存。

---

## 单章写入流程

> 详见 `references/chapter-writing-workflow.md`（完整流程、追踪文件清单、常见错误）

**正确流程（禁止跳过，必须先规划再写）：**

**【新增】写前规划（必做）：**
1. 确认目标字数和合格范围（设定值±20%）
2. 脑中规划内容结构：本章核心事件、分几个场景/段落、每个段落大约多少字
3. 估算总字数是否在合格范围内
4. 如果估算超出上限30%或低于下限20%，先调整内容再写
5. **严禁**：先写8000字再砍到3000，或先写1500字再扩充到3000

**写章节：**
1. `write_file` 写入正文（完整内容，不截断）
2. **字数验证**（必须执行，不通过不得写下一章）：运行验证脚本
   ```bash
   python3 ~/.hermes/skills/creative/novel-worldbuilder/scripts/verify_chapter.py <章节文件> <设定字数>
   ```
   例：`python3 .../verify_chapter.py 正文/卷二_对抗与成长/第31章.md 3000`
   返回0=合格，返回1=不合格（需扩充后重新验证）。
3. `terminal` 检查文件行数，确认保存成功
4. 对话中输出审核报告（不保存）
5. 立即更新所有追踪文件
6. 验证追踪文件关键行
7. 向用户报告完成

**禁止做法**：
- ❌ 把审核报告写在章节正文同一文件里
- ❌ 用一次 `write_file` 调用同时写入正文和审核报告
- ❌ 在章节文件末尾追加审核内容
- ❌ 写完章节后不立即更新追踪文件
- ❌ **写章节时使用 `...[truncated]` / `待续` / `未完` 等截断标记**（write_file 只写入你给它的确切文本，不会自动补全）
- ❌ **字数严重不足（低于设定值80%）就写下一章**（每章字数以项目设定值为准，±20%可接受）

**截断标记事故的后果**：写入的内容只包含截断位置之前的文字，导致章节只有几百字而非预期的3000字，且文件已保存，无法通过重新写入恢复（除非手动重写）。

**正确做法**：写章节前先在大脑中或草稿中组织好完整内容，然后一次性写入。字节数/字数验证必须在 `write_file` 返回后立即执行。

**事故修复（当审核报告已混入文件）**：
```bash
# 1. 定位审核报告起始行
grep -n "第X章审核报告" 正文/卷一_XXX/第X章.md
# 2. 裁剪文件（审核报告从第N行开始，则保留前N-1行）
head -n 176 正文/卷一_XXX/第X章.md > /tmp/chX_clean.md && mv /tmp/chX_clean.md 正文/卷一_XXX/第X章.md
# 3. 清理残留分隔符
sed -i '/^---$/d' 正文/卷一_XXX/第X章.md
# 绝对禁止用 tail 裁剪——tail 保留末尾删除开头，是反向操作
```

## 目录结构（初始化时由 init_novel.py 创建）

```
~/hermes/novels/《书名》/
├── 世界观/（世界设定/时代背景/力量体系/世界状态.md）
├── 势力/（势力分布.md）
├── 人物/（角色注册表/人物关系图/关系记录/伏笔钩子/其他人物.md）
├── 大纲/（剧情大纲/分卷大纲/章节大纲/编年史/剧情线追踪/角色弧光追踪/进度看板.md）
├── 写作指南/（文风指南/写作规范.md）
└── 正文/（卷一/卷二/卷三/）
```

### 动态更新机制

```markdown
# 角色注册表

> 自动维护，随新角色添加而更新 | 更新时间：2025-01-01

| ID | 姓名 | 别名/称号 | 势力 | 主角团 | 状态 | 首次出场 | 档案路径 |
|----|------|----------|------|--------|------|----------|----------|
| C001 | 姓名 | 别名 | 势力名 | ★ | 活跃 | 第X卷 | 角色档案/主角.md |
| C002 | 姓名 | 别名 | 势力名 | ○ | 死亡 | 第X卷 | 角色档案/角色A.md |

- 主角团：★ = 主角团成员，○ = 非主角团
- 状态：活跃 / 死亡 / 退场 / 失踪 / 转化(反派)
```

### 角色档案/[角色名].md

```markdown
# 角色名

> 文件路径：角色档案/角色名.md | 最后更新：2025-01-01

## 基本信息
- **ID**: C002
- **出场年龄**: XX岁
- **表面身份**: XXX
- **真实身份**: XXX
- **势力**: XXX

## 外貌
描述

## 性格
- 特质：
- 优点：
- 缺陷：

## 核心
- **欲望**：最想要什么
- **恐惧**：最怕什么

## 人物弧光
从故事开始到结束经历的变化

## 关系
| 角色 | 关系类型 | 备注 |
|------|---------|------|
| 主角 | 导师/盟友/宿敌 | 关系描述 |

## 出场记录
- 首次出场：第X卷 第X章
- 重要戏份：第X卷 第X章（事件）
```

### 关系记录.md

```markdown
# 关系变化记录

> 每次关系变化追加一条记录，不要修改旧记录

## 2025-01-01 更新
- **A 与 B**：从「陌生」变为「盟友」，原因：XXXX
- **主角 与 C**：从「敌对」变为「和解」，原因：XXXX

## 2025-01-02 更新
- **新角色 D**：加入组织 XXX，身份为卧底
- **A 与 B**：从「盟友」变为「决裂」，原因：XXXX
```

### 人物关系图.md

```markdown
# 人物关系图

> 随关系变化更新 | 更新时间：2025-01-01

## ASCII 关系网络（以主角为中心）

         [导师/引路人 C]
                  │
              (师徒/恩人)
                  │
[盟友 B] ──── [★主角 A★] ──── [恋人/羁绊 D]
                  │
              (宿敌/对抗)
                  │
              [对手/反派 E]
                  │
              (幕后操控)
                  │
             [隐藏黑手 F]

## 势力关系图

[正道联盟] ←——— 对抗 ———→ [魔教]
   │  │                       │  │
掌门 师兄 师妹           教主  护法  卧底
| 一次 write_file 多段内容 | 写入不完整 | 分开多次调用 |
| 写完章节后用 head -n 裁剪时行数算错 | 审核报告残留，或正文被截断 | 见下文「事故修复：当审核报告已混入文件」 |

---

## 事故修复：当审核报告已混入文件

如果在写入时不小心把审核报告写进了章节文件，用以下步骤清理：

**步骤1：定位审核报告起始行**
```bash
grep -n "第X章审核报告" 正文/卷一_XXX/第X章.md
```
输出示例：`177:## 第X章审核报告` → 审核报告从第177行开始

**步骤2：裁剪文件**
```bash
head -n 176 正文/卷一_XXX/第X章.md > /tmp/chX_clean.md
mv /tmp/chX_clean.md 正文/卷一_XXX/第X章.md
```

**步骤3：清理残留的分隔符**
```bash
sed -i '/^---$/d' 正文/卷一_XXX/第X章.md
```

**步骤4：验证**
```bash
wc -l 正文/卷一_XXX/第X章.md
tail -3 正文/卷一_XXX/第X章.md
```

**绝对禁止**：用 `tail` 来裁剪文件——它会保留文件末尾而删除开头，是反向操作。

---

## 批量更新技巧

写完多章后一次性更新所有追踪文件：

1. 先写完所有章节（6-10章）
2. 确认每章文件都干净（末尾是正文，无审核报告残留）
3. 读取所有追踪文件当前状态（一次 read_file 多个）
4. 批量 patch/write_file 所有追踪文件
5. 最终一次验证

---

*最后更新：2025-05-09（增加事故修复 + 批量更新技巧）*

### 步骤一：理解需求

1. 接收用户剧情梗概
2. 判断信息量：
   - **≥50字且有完整情节点** → 进入"步骤一补充：确认创作参数"
| 一次 write_file 多段内容 | 写入不完整 | 分开多次调用 |
| 写完章节后用 head -n 裁剪时行数算错 | 审核报告残留，或正文被截断 | 见下文「事故修复：当审核报告已混入文件」 |

---

## 事故修复：当审核报告已混入文件

如果在写入时不小心把审核报告写进了章节文件，用以下步骤清理：

**步骤1：定位审核报告起始行**
```bash
grep -n "第X章审核报告" 正文/卷一_XXX/第X章.md
```
输出示例：`177:## 第X章审核报告` → 审核报告从第177行开始

**步骤2：裁剪文件**
```bash
head -n 176 正文/卷一_XXX/第X章.md > /tmp/chX_clean.md
mv /tmp/chX_clean.md 正文/卷一_XXX/第X章.md
```

**步骤3：清理残留的分隔符**
```bash
sed -i '/^---$/d' 正文/卷一_XXX/第X章.md
```

**步骤4：验证**
```bash
wc -l 正文/卷一_XXX/第X章.md
tail -3 正文/卷一_XXX/第X章.md
```

**绝对禁止**：用 `tail` 来裁剪文件——它会保留文件末尾而删除开头，是反向操作。

---

## 批量更新技巧

写完多章后一次性更新所有追踪文件：

1. 先写完所有章节（6-10章）
2. 确认每章文件都干净（末尾是正文，无审核报告残留）
3. 读取所有追踪文件当前状态（一次 read_file 多个）
4. 批量 patch/write_file 所有追踪文件
5. 最终一次验证

---

*最后更新：2025-05-09（增加事故修复 + 批量更新技巧）*

**必须与用户确认以下5个参数**，等待用户回复后再进入步骤二：

```
📋 创作参数确认清单

请确认（或由我推荐）以下创作参数：

1️⃣ 人称/视角
   - A. 第三人称限定（紧跟主角）← 推荐，系统流/修仙流常用
   - B. 第三人称全知
   - C. 第一人称
   - D. 你有其他想法？

2️⃣ 每章字数（自定义）
   - A. 1500-2000 字（快节奏/短平快）
   - B. 2500-3500 字（主流网文标准）← 推荐
   - C. 4000-6000 字（长篇/付费向）
   - D. 其他自定义字数？（请直接填写数字，如"每章4000字"）
   
   **⚠️ 重要说明**：每章字数是自定义参数，非固定值。实际写作时允许在设定值 ±20% 范围内浮动（如设定3000字/章，则2400-3600字均可接受）。

3️⃣ 章节总数 / 作品体量
   - A. 30-50章（短篇，10-15万字）
   - B. 80-120章（中篇，24-36万字）← 推荐
   - C. 150章+（长篇，45万字+）← 推荐（本技能默认）
   - D. 你有其他想法？

4️⃣ 作品基调
   - A. 搞笑/沙雕
   - B. 热血/燃
   - C. 黑暗/压抑
   - D. 治愈/日常
   - E. 你有其他想法？

5️⃣ 发布平台
   - A. 起点
   - B. 晋江
   - C. 番茄
   - D. 飞卢
   - E. 不发布（自用）
   - F. 你有其他想法？
```

**格式要求**：必须逐条列出，用 A/B/C/D 选项让用户选择或回复。不得跳过此步骤直接进入生成。

| 一次 write_file 多段内容 | 写入不完整 | 分开多次调用 |
| 写完章节后用 head -n 裁剪时行数算错 | 审核报告残留，或正文被截断 | 见下文「事故修复：当审核报告已混入文件」 |

---

## 事故修复：当审核报告已混入文件

如果在写入时不小心把审核报告写进了章节文件，用以下步骤清理：

**步骤1：定位审核报告起始行**
```bash
grep -n "第X章审核报告" 正文/卷一_XXX/第X章.md
```
输出示例：`177:## 第X章审核报告` → 审核报告从第177行开始

**步骤2：裁剪文件**
```bash
head -n 176 正文/卷一_XXX/第X章.md > /tmp/chX_clean.md
mv /tmp/chX_clean.md 正文/卷一_XXX/第X章.md
```

**步骤3：清理残留的分隔符**
```bash
sed -i '/^---$/d' 正文/卷一_XXX/第X章.md
```

**步骤4：验证**
```bash
wc -l 正文/卷一_XXX/第X章.md
tail -3 正文/卷一_XXX/第X章.md
```

**绝对禁止**：用 `tail` 来裁剪文件——它会保留文件末尾而删除开头，是反向操作。

---

## 批量更新技巧

写完多章后一次性更新所有追踪文件：

1. 先写完所有章节（6-10章）
2. 确认每章文件都干净（末尾是正文，无审核报告残留）
3. 读取所有追踪文件当前状态（一次 read_file 多个）
4. 批量 patch/write_file 所有追踪文件
5. 最终一次验证

---

*最后更新：2025-05-09（增加事故修复 + 批量更新技巧）*

**第一步：生成书名（必须提供至少5个选项）**

根据剧情风格生成 5-7 个候选书名（用「」包裹），以列表形式呈现给用户选择。

**书名生成规则：**
- 书名必须与剧情核心元素相关（主角身份、退婚、系统、宗门等）
- 同时体现作品基调（搞笑/热血/黑暗等）
- 格式统一用「」，例如「摆烂修仙从被退婚开始」
- 提供选项后，询问用户：「以上都不满意？可输入自定义书名」

**示例格式：**
```
📖 候选书名：

1. 「摆烂修仙从被退婚开始」← 推荐，最契合退婚+系统+搞笑元素
2. 「咸鱼赘婿的逆袭之路」
3. 「我只想躺着修炼」
4. 「废物仙途」
5. 「天道系统逼我上清华」
6. 「我懒我有理」

以上都不满意？可输入自定义书名。
```

**第二步：确认书名后，创建目录骨架**

用户选择或输入书名后：
1. 调用 `scripts/init_novel.py` 创建完整目录结构（只创建骨架，不生成内容）
2. 向用户报告目录树

**第三步：预览确认（新增）**

目录骨架创建后、生成正文之前，**先输出预览摘要供用户确认**，内容包括：

```
## 📋 小说设定预览 —《[书名]》

### 🌍 世界观概要
[基于梗概扩展的2-3段世界观描述：时代背景、修炼体系特点、社会结构]

### ⚔️ 势力分布
[主要势力列表，每方势力1-2句话说明立场和特点]

### 👤 主要角色设定
**[主角]**：[名字/身份/性格/核心冲突]
**[女主群像]**：[每个女主1句话特点定位]
**[关键配角]**：[2-3个重要配角的简要设定]

### 📖 剧情主线预览
[用时间线/章节阶段方式展示大概走向，含3-5个关键转折点]

### 🎯 伏笔与悬念
[埋下的主要伏笔钩子，2-4个]

---
**请确认以上设定是否满意：**
- ✅ 满意，开始生成全部设定文件
- 📝 修改：[指出需要修改的部分]
- ❌ 放弃创建
```
> ⚠️ 这一步**不生成任何文件**，只输出预览摘要。确认后才开始生成正文。

---

**第四步：逐文件生成内容（确认后执行）**

预览确认后，**按以下顺序逐文件生成内容**。每个文件生成并写入磁盘后，才开始生成下一个。

> ⚠️ **禁止用子代理批量生成所有文件**。必须逐文件生成，每次生成1个文件，写入后再生成下一个。
>
> ⚠️ 批量生成失败的根因分析见：`references/delegate-batch-failure.md`
>
> ⚠️ **章节写入规则**：`references/chapter-writing-workflow.md` — 章节文件只写正文，审核报告不保存

**支持文件速查**：
- `references/chapter-writing-workflow.md` — 章节写入与追踪更新流程（含常见错误表）
- `references/chapter-completion-checklist.md` — 20项审核标准
- `references/writing-quick-ref.md` — 写作速查（字数/POV/禁用词）
| 一次 write_file 多段内容 | 写入不完整 | 分开多次调用 |
| 写完章节后用 head -n 裁剪时行数算错 | 审核报告残留，或正文被截断 | 见下文「事故修复：当审核报告已混入文件」 |

---

## 事故修复：当审核报告已混入文件

如果在写入时不小心把审核报告写进了章节文件，用以下步骤清理：

**步骤1：定位审核报告起始行**
```bash
grep -n "第X章审核报告" 正文/卷一_XXX/第X章.md
```
输出示例：`177:## 第X章审核报告` → 审核报告从第177行开始

**步骤2：裁剪文件**
```bash
head -n 176 正文/卷一_XXX/第X章.md > /tmp/chX_clean.md
mv /tmp/chX_clean.md 正文/卷一_XXX/第X章.md
```

**步骤3：清理残留的分隔符**
```bash
sed -i '/^---$/d' 正文/卷一_XXX/第X章.md
```

**步骤4：验证**
```bash
wc -l 正文/卷一_XXX/第X章.md
tail -3 正文/卷一_XXX/第X章.md
```

**绝对禁止**：用 `tail` 来裁剪文件——它会保留文件末尾而删除开头，是反向操作。

---

## 批量更新技巧

写完多章后一次性更新所有追踪文件：

1. 先写完所有章节（6-10章）
2. 确认每章文件都干净（末尾是正文，无审核报告残留）
3. 读取所有追踪文件当前状态（一次 read_file 多个）
4. 批量 patch/write_file 所有追踪文件
5. 最终一次验证

---

**第四步：逐文件生成内容（确认后执行）**

预览确认后，**按以下顺序逐文件生成内容**。每个文件生成并写入磁盘后，才开始生成下一个。

**文件生成顺序（共19个文件）：**

```
第1批（核心设定）
① 设定集.md              ← 主入口，完整六模块
② 世界观/世界观设定.md
③ 世界观/时代背景.md
④ 世界观/力量体系.md

第2批（世界状态）
⑤ 世界观/世界状态.md

第3批（势力与人物基础）
⑥ 势力/势力分布.md
⑦ 人物/角色注册表.md
⑧ 人物/人物关系图.md

第4批（人物详情）
⑨ 人物/关系记录.md
⑩ 人物/伏笔与钩子.md
⑪ 人物/其他人物.md
⑫ 人物/角色档案/主角.md

第5批（后宫角色档案）
⑬ 人物/角色档案/苏瑶.md
⑭ 人物/角色档案/姜雪.md
⑮ 人物/角色档案/叶琉璃.md
⑯ 人物/角色档案/萧青竹.md
⑰ 人物/角色档案/[其余女性角色].md   （直到全部完成）

第6批（大纲与指南）
⑱ 大纲/剧情大纲.md
⑲ 大纲/分卷大纲.md
⑳ 大纲/章节大纲.md（含前5章示例）
㉑ 大纲/编年史.md
㉒ 写作指南/文风指南.md
㉓ 写作指南/写作规范.md
㉔ README.md
```

*最后更新：2025-05-10（增加预览确认步骤 — 第三步）*

**生成单个文件的工作流：**
1. 读取 `references/` 下的参考模板（如 `structure-guide.md`）
2. 根据用户剧情梗概和已确认的参数，生成该文件内容
3. 用 `write_file` 写入磁盘
4. 向用户报告：`[1/19] 已完成：设定集.md`
5. 开始生成下一个

**⚠️ 绝对禁止以下做法：**
- ❌ 用 `delegate_task` 同时调用多个子代理批量生成所有文件（会全部超时）
- ❌ 让一个子代理一次性生成数万字内容（API 超时）
- ❌ 在多轮对话中分散生成文件（上下文断裂导致前后矛盾）
- ✅ 只用主 Agent 逐文件生成，每次 1 个，写入后再生成下一个
- ✅ 主 Agent 在同一上下文中累积生成所有文件，引用已生成内容保证一致性

**生成完毕后：**
| 一次 write_file 多段内容 | 写入不完整 | 分开多次调用 |
| 写完章节后用 head -n 裁剪时行数算错 | 审核报告残留，或正文被截断 | 见下文「事故修复：当审核报告已混入文件」 |

---

## 事故修复：当审核报告已混入文件

如果在写入时不小心把审核报告写进了章节文件，用以下步骤清理：

**步骤1：定位审核报告起始行**
```bash
grep -n "第X章审核报告" 正文/卷一_XXX/第X章.md
```
输出示例：`177:## 第X章审核报告` → 审核报告从第177行开始

**步骤2：裁剪文件**
```bash
head -n 176 正文/卷一_XXX/第X章.md > /tmp/chX_clean.md
mv /tmp/chX_clean.md 正文/卷一_XXX/第X章.md
```

**步骤3：清理残留的分隔符**
```bash
sed -i '/^---$/d' 正文/卷一_XXX/第X章.md
```

**步骤4：验证**
```bash
wc -l 正文/卷一_XXX/第X章.md
tail -3 正文/卷一_XXX/第X章.md
```

**绝对禁止**：用 `tail` 来裁剪文件——它会保留文件末尾而删除开头，是反向操作。

---

## 批量更新技巧

写完多章后一次性更新所有追踪文件：

1. 先写完所有章节（6-10章）
2. 确认每章文件都干净（末尾是正文，无审核报告残留）
3. 读取所有追踪文件当前状态（一次 read_file 多个）
4. 批量 patch/write_file 所有追踪文件
5. 最终一次验证

---

*最后更新：2025-05-09（增加事故修复 + 批量更新技巧）*

当用户要求添加新角色时，执行以下操作：

1. **生成新角色档案** → 写入 `角色档案/[角色名].md`
2. **追加到角色注册表** → `角色注册表.md` 新增一行（ID自动递增）
3. **追加到关系记录** → `关系记录.md` 写入新角色与现有角色的初始关系
4. **更新人物关系图** → 重新绘制 ASCII 图，将新角色纳入
5. **如涉及势力** → 更新 `势力/势力分布.md`

### 步骤五：剧情推进中——更新关系

当用户描述关系变化时：

1. **追加到关系记录** → `关系记录.md` 写入变更（保留历史）
2. **更新双方角色档案** → 关系字段同步更新
3. **重新绘制关系图** → `人物关系图.md`

### 步骤六：剧情推进中——角色状态变化

当角色死亡、退场、转化时：

1. **更新角色注册表** → 状态列标记
2. **更新角色档案** → 结局字段标注
3. **追加到关系记录** → 记录死亡/退场事件
| 一次 write_file 多段内容 | 写入不完整 | 分开多次调用 |
| 写完章节后用 head -n 裁剪时行数算错 | 审核报告残留，或正文被截断 | 见下文「事故修复：当审核报告已混入文件」 |

---

## 事故修复：当审核报告已混入文件

如果在写入时不小心把审核报告写进了章节文件，用以下步骤清理：

**步骤1：定位审核报告起始行**
```bash
grep -n "第X章审核报告" 正文/卷一_XXX/第X章.md
```
输出示例：`177:## 第X章审核报告` → 审核报告从第177行开始

**步骤2：裁剪文件**
```bash
head -n 176 正文/卷一_XXX/第X章.md > /tmp/chX_clean.md
mv /tmp/chX_clean.md 正文/卷一_XXX/第X章.md
```

**步骤3：清理残留的分隔符**
```bash
sed -i '/^---$/d' 正文/卷一_XXX/第X章.md
```

**步骤4：验证**
```bash
wc -l 正文/卷一_XXX/第X章.md
tail -3 正文/卷一_XXX/第X章.md
```

**绝对禁止**：用 `tail` 来裁剪文件——它会保留文件末尾而删除开头，是反向操作。

---

## 批量更新技巧

写完多章后一次性更新所有追踪文件：

1. 先写完所有章节（6-10章）
2. 确认每章文件都干净（末尾是正文，无审核报告残留）
3. 读取所有追踪文件当前状态（一次 read_file 多个）
4. 批量 patch/write_file 所有追踪文件
5. 最终一次验证

---

*最后更新：2025-05-10（增加章节写前规划步骤，避免来回修改）*

---

#### 章节写前规划（重要！必须先规划再写）

> 写之前必须先规划章节结构，生成时按规划执行。**一次写完，写完验证，不来回修改**。

**步骤1：规划结构**

写之前，先规划本章框架：

```
第X章 章节名

- 开头（约300字）：时间/地点/人物状态，建立场景
- 中段（约1600字）：事件发展、对话、冲突
- 高潮（约800字）：核心冲突爆发
- 结尾（约300字）：悬念/转折/情感升华

总计：约3000字（±20%，即2400-3600）
```

**步骤2：生成时做字数中间点检查**

⚠️ **只规划不够，必须在生成过程中检查字数**。不要写完再检查——那时候补救已经被动。

- 每写完一个段落（约500-800字），快速估算已写多少字
- 用 `wc -m` 实时查看当前字符数
- **目标：写完全部内容后，预估能达到设定值×1.1以上**（留10%buffer，因为实际输出往往比预估少10-20%）
- 如果中间点检查发现字数不足预期的60%，立即扩充当前段落再继续

**步骤3：写完立即验证字数**

```bash
wc -m 正文/卷X_XXX/第X章.md | awk '{print $1}'
```

- 若在2400-3600之间：✅ 完成
- 若<2400：直接补充关键情感到结尾（补200-400字比写完再扩充容易得多）
- 若>3600：直接删减冗余描写

**步骤4：报告完成**

字数合格后，报告"第X章：XXXX字 ✅"，继续写下一章。

> ⚠️ **绝对禁止**：写之前不规划、边写边发挥、发现字数不对来回修改。这样既浪费输出token，又浪费时间。
>
> ⚠️ **绝对禁止截断标记**：`...`、`待续`、`未完`、`[TBC]`等。`write_file`只写入给定文本，不会自动补全。
>
> ⚠️ **已知陷阱**：实际输出往往比预估少10-20%。如果规划3000字，预估时要按3300-3500的标准写，否则写出来很可能只有2400-2600。本session教训：ch7规划2800→实写2351，ch8规划2800→实写2070，ch11实写仅1941。

**【重要】写入截断问题的解决方案**：

已知系统限制：`write_file`单次写入超过约1000字符会截断，`python3 -c`单行脚本命令太长会失败。

**解决方案：按剧情自然分段写入**

1. 用`write_file`写入第一段剧情（约800-1500字）
2. 用`patch(new_string=新段落, old_string=衔接句)`追加下一段
3. 重复直到章节完成，每段长度根据剧情自然段落决定

**示例流程（第21章）**：
```
① write_file 第一段（大比开幕+开打，~800字）
② patch 追加第二段（苦战+险胜，~800字）  
③ patch 追加第三段（赛后+第二轮，~600字）
④ patch 追加第四段（第三轮对手预告，~400字）
→ 验证：wc -m 第21章.md → 2545字符 ✅
```

---

执行写前审查后，按以下格式生成正文：

#### 每章内容模板

```markdown
# 第X章：章节名

[正文内容，符合以下规范]
```

#### 正文写作规范

1. **开头**：直接切入场景，第一句点名时间/地点/人物状态
2. **节奏**：
   - 第1章：建立处境（被退婚/穿越等），让读者有代入感
   - 中间章节：事件→冲突→解决，每章至少一个核心冲突
   - 结尾：必须有一个钩子（悬念/意外/笑点），不能平淡收尾
3. **对话**：「」包裹，口语化
4. **心理**：用吐槽代替"他感到…"，主角的内心OS要有性格
5. **笑点密度**：每章至少2-3个有效笑点，不能重复同一个梗
6. **系统吐槽**（如果适用）：【系统：…】格式
7. **禁止**：赫然/陡然/突然（主角是懒人，动作要慢）
8. **字数**：每章 [自定义字数] 字（±20%浮动，实际浮动范围根据项目设定值计算）

#### ⚠️ 章节文件写入规则（必须遵守）

> 写章节时最容易犯的错误是把「正文」和「审核报告」混在一起保存，以及忘记更新追踪文件。

**正确流程**：
1. `write_file` 写入正文（不含审核报告）
2. `terminal` 检查文件行数/大小，确认保存成功
3. 在对话中输出审核报告（不保存）
4. 立即执行追踪文件更新（patch/write_file）
5. `terminal` 检查追踪文件中的关键行，确认更新成功
6. 向用户报告完成

**错误做法**：
- ❌ 把审核报告写在章节正文同一文件里
- ❌ 用一次 `write_file` 调用同时写入正文和审核报告
- ❌ 在章节文件末尾追加审核内容
- ❌ 写完章节后不立即更新追踪文件，等用户说"更新"才做

**示例（正确流程）**：
```
# 1. 写入正文
write_file(path="正文/卷一_铺垫与启程/第5章.md", content=[仅正文])

# 2. 验证文件保存
terminal(command="wc -l 正文/卷一_铺垫与启程/第5章.md")
→ 确认行数 > 0

# 3. 输出审核报告（对话中，不保存）

# 4. 更新追踪文件
patch(new_string="第5章", old_string="第6章（待写）", path="大纲/章节大纲.md")
patch(... 其他追踪文件 ...)

# 5. 验证追踪文件更新
terminal(command="grep '第5章' 大纲/章节大纲.md")
→ 确认出现"✅已完成"

#### 防止重复写作规范（重要！）

**五大常见重复问题：**

**问题一：同一个借口说太多遍**
- ❌ 连续用"病了"/"不想动"/"躺着"超过3次
- ✅ 用一次拒绝后，立刻推进场景（对方进来了/换策略了/时间紧迫了）

**问题二：原地打转不推进**
- ❌ 主角拒绝一件事，反复拉扯几十行
- ✅ 拒绝一次或两次后，要么妥协去办，要么有外部事件打断，剧情必须往前走

**问题三：反应模式单一**
- ❌ 除了"躺"和"怼"没有其他动作
- ✅ 主角可以有：站起来、走、观察环境、沉默、皱眉、叹气、主动问问题等多样反应

**问题四：对话全是同一套路**
- ❌ 对方说什么，主角就怼什么，句式固定
- ✅ 对话要有变化：有时正常回答，有时反问，有时沉默，有时出其不意同意对方

**问题五：笑点重复**
- ❌ 同一个梗翻来覆去用
- ✅ 每章要有2-3个不同类型的笑点：主角懒逻辑、意外反转、配角崩溃、系统吐槽等

**实操口诀：**
1. 拒绝一件事最多说两遍，第三遍必须变
2. 场景拉扯不超过五行，必须有事件打断或推进
3. 主角反应要有变化，不能只会躺和怼
4. 对话要有节奏变化，有时短有时长
5. 每章必须有新信息进入，不能全是重复内容

#### 章节内笑点类型参考

```
- 主角用懒人逻辑怼人（正经说废话）
- 系统吐槽（如果适用）
- 配角被主角气到无语
- 意外的反转（期待vs现实落差）
- 主角把正经场合往躺平方向带
```

#### 写后更新（必做 — 不要等待用户触发）

每章写完后，**立即自动执行**以下更新操作，不要等用户说"更新"：

| 操作 | 更新文件 | 格式 |
|------|---------|------|
| 章节内容 | `正文/卷X_XXX/第Y章.md` | 完整正文 |
| 章节摘要 | `大纲/章节大纲.md` | 追加本章：字数/核心事件/场景/线条/伏笔/状态变化 |
| 时间线 | `大纲/编年史.md` | 追加：故事内时间点 + 事件 + 章节号 |
| 世界状态 | `世界观/世界状态.md` | 更新人物位置/状态/物品位置 |
| 伏笔埋设 | `人物/伏笔与钩子.md` | 追加新伏笔（Fxxx）/ 线索 / 钩子 |
| 剧情线追踪 | `大纲/剧情线追踪.md` | 追加本章推进的线条记录 |
| 进度看板 | `大纲/进度看板.md` | 更新：已完成章节数+1、累计字数 |
| 角色弧光追踪 | `大纲/角色弧光追踪.md` | 记录本章角色弧光变化 |
| 关系变化 | `人物/关系记录.md` | 如有变化，追加变更记录 |
| 角色档案 | `人物/角色档案/[角色名].md` | 追加出场记录 |

#### 追踪文件完整性检查

每次更新前，检查目标文件是否有：
- 重复的章节条目（如两个"第2章"）→ 删除旧条目
- 过时的占位符（如"🔄待写"但章节已写完）→ 更新为"✅已完成"
- 空值占位符（如"字数：待定"但字数已知）→ 填充实际值

如果发现冗余条目，在更新时一并清理。

### 步骤八：章节自动审核

每章正文写完后，**必须自动执行**以下审核流程：
1. 读取刚写的章节文件
2. 对照 `references/chapter-completion-checklist.md` 的 20 项标准，逐项输出结果到对话
3. 审核报告**只在对话中输出**，不写入任何文件
4. 审核通过后，向用户报告完成并询问是否继续

> ⚠️ 审核报告不在对话中输出是严重错误——用户需要看到审核结果才能判断章节质量

> ⚠️ 审核报告只在对话中输出，**不要**写入章节文件内

#### 审核输出格式

```
## 第X章审核报告
### 一、基础合规（4项）—— 必须100%通过
✅/❌ 1. 字数：XXX字（设定值±20%合格，如设定3000字则2400-3600合格；审核时根据实际设定值计算）
✅/❌ 2. POV：仅跟主角/有POV切换（列出切换位置）
✅/❌ 3. 对话格式：全部「」/存在「和"混用
✅/❌ 4. 禁用词：无赫然/陡然/突然/赫然/有（列出位置）

### 二、剧情质量（5项）—— 必须100%通过
✅/❌ 5. 有核心事件：本章事件是XXX
✅/⚠️ 6. 推进线条：L01(✓) L03(✓) L04(×)（列出线条状态）
⚠️/❌ 7. 无重复：有/无重复（重复类型：借口/场景/对话）
✅/❌ 8. 章末钩子：有（类型）/无
✅/❌ 9. 新信息：本章引入XXX

### 三、伏笔检查（3项）
✅/⚠️/❌ 10. 伏笔登记：新埋伏笔已登记Fxxx/未登记
✅/⚠️/❌ 11. 伏笔一致性：与已有设定一致/有矛盾（说明）
✅/❌ 12. 伏笔密度：X个（≤2合格/超过）

### 四、场景一致性（3项）
✅/⚠️/❌ 13. 场景衔接：与第X章衔接一致/有跳跃
✅/⚠️/❌ 14. 人物位置：与上章末尾一致/有矛盾（说明）
✅/⚠️/❌ 15. 时间线：时间不早于上章/有倒流

### 五、追踪文件更新（5项）
✅/❌ 16. 章节大纲：已追加/未追加
✅/❌ 17. 编年史：已追加/未追加
✅/❌ 18. 世界状态：已更新/未更新
✅/❌ 19. 剧情线追踪：已记录/未记录
| 一次 write_file 多段内容 | 写入不完整 | 分开多次调用 |
| 写完章节后用 head -n 裁剪时行数算错 | 审核报告残留，或正文被截断 | 见下文「事故修复：当审核报告已混入文件」 |

---

## 事故修复：当审核报告已混入文件

如果在写入时不小心把审核报告写进了章节文件，用以下步骤清理：

**步骤1：定位审核报告起始行**
```bash
grep -n "第X章审核报告" 正文/卷一_XXX/第X章.md
```
输出示例：`177:## 第X章审核报告` → 审核报告从第177行开始

**步骤2：裁剪文件**
```bash
head -n 176 正文/卷一_XXX/第X章.md > /tmp/chX_clean.md
mv /tmp/chX_clean.md 正文/卷一_XXX/第X章.md
```

**步骤3：清理残留的分隔符**
```bash
sed -i '/^---$/d' 正文/卷一_XXX/第X章.md
```

**步骤4：验证**
```bash
wc -l 正文/卷一_XXX/第X章.md
tail -3 正文/卷一_XXX/第X章.md
```

**绝对禁止**：用 `tail` 来裁剪文件——它会保留文件末尾而删除开头，是反向操作。

---

## 批量更新技巧

写完多章后一次性更新所有追踪文件：

1. 先写完所有章节（6-10章）
2. 确认每章文件都干净（末尾是正文，无审核报告残留）
3. 读取所有追踪文件当前状态（一次 read_file 多个）
4. 批量 patch/write_file 所有追踪文件
5. 最终一次验证

---

*最后更新：2025-05-09（增加事故修复 + 批量更新技巧）*
→ ✅ 全部通过，可以标记完成
→ ⚠️ 有可改进项（列出），确认后标记完成
→ ❌ 有硬性未通过项（列出），必须先修复再标记完成
```

#### 审核执行规则

1. **自动判定项**（1/2/3/4/7/8/9/12/16/17/18/19/20）：读取章节内容后直接判定
2. **需查询文件项**（5/6/10/11/13/14）：同时读取 `大纲/章节大纲.md`、`大纲/剧情线追踪.md`、`人物/伏笔与钩子.md`、`世界观/世界状态.md` 等文件进行交叉验证
3. **未通过处理**：在审核结论后列出具体修复指令，格式为 `→ 修复：[问题]（操作建议）`
4. **审核后才能标记完成**：章节大纲中标记「✅已完成」必须在审核通过之后

#### 每章审核优先级

| 优先级 | 项数 | 说明 |
|--------|------|------|
| P0（硬性） | 第1-9项 | 任何一项未通过，章节不能标记完成 |
| P1（伏笔） | 第10-12项 | 影响故事完整性和后续创作，必须修复 |
| P2（一致） | 第13-15项 | 建议通过，有矛盾会累积成大问题 |
| 一次 write_file 多段内容 | 写入不完整 | 分开多次调用 |
| 写完章节后用 head -n 裁剪时行数算错 | 审核报告残留，或正文被截断 | 见下文「事故修复：当审核报告已混入文件」 |

---

## 事故修复：当审核报告已混入文件

如果在写入时不小心把审核报告写进了章节文件，用以下步骤清理：

**步骤1：定位审核报告起始行**
```bash
grep -n "第X章审核报告" 正文/卷一_XXX/第X章.md
```
输出示例：`177:## 第X章审核报告` → 审核报告从第177行开始

**步骤2：裁剪文件**
```bash
head -n 176 正文/卷一_XXX/第X章.md > /tmp/chX_clean.md
mv /tmp/chX_clean.md 正文/卷一_XXX/第X章.md
```

**步骤3：清理残留的分隔符**
```bash
sed -i '/^---$/d' 正文/卷一_XXX/第X章.md
```

**步骤4：验证**
```bash
wc -l 正文/卷一_XXX/第X章.md
tail -3 正文/卷一_XXX/第X章.md
```

**绝对禁止**：用 `tail` 来裁剪文件——它会保留文件末尾而删除开头，是反向操作。

---

## 批量更新技巧

写完多章后一次性更新所有追踪文件：

1. 先写完所有章节（6-10章）
2. 确认每章文件都干净（末尾是正文，无审核报告残留）
3. 读取所有追踪文件当前状态（一次 read_file 多个）
4. 批量 patch/write_file 所有追踪文件
5. 最终一次验证

---

*最后更新：2025-05-09（增加事故修复 + 批量更新技巧）*

**章节大纲追加格式：**
```markdown
### 第X章：章节名
- 状态：✅已完成 / 🔄草稿（写完后改为✅已完成）
- 字数：实际字数
- 核心事件：本章发生的主要事件
- 场景：御书房 / 十王府破院 / 春猎现场（与前后章衔接）
- 推进线条：L01（✓）、L03（×）（标记本章推进了哪些线条）
- 埋下伏笔：Fxxx（如有）
- 世界状态变化：本章人物/物品/地点的变化
```

**编年史追加格式：**
```markdown
| 故事时间点 | 事件概要 | 对应章节 |
|-----------|---------|---------|
| 第26天 | 春猎文试，二皇子挑衅，魏忠海出现，三皇子接触 | 第7-8章 |
| 一次 write_file 多段内容 | 写入不完整 | 分开多次调用 |
| 写完章节后用 head -n 裁剪时行数算错 | 审核报告残留，或正文被截断 | 见下文「事故修复：当审核报告已混入文件」 |

---

## 事故修复：当审核报告已混入文件

如果在写入时不小心把审核报告写进了章节文件，用以下步骤清理：

**步骤1：定位审核报告起始行**
```bash
grep -n "第X章审核报告" 正文/卷一_XXX/第X章.md
```
输出示例：`177:## 第X章审核报告` → 审核报告从第177行开始

**步骤2：裁剪文件**
```bash
head -n 176 正文/卷一_XXX/第X章.md > /tmp/chX_clean.md
mv /tmp/chX_clean.md 正文/卷一_XXX/第X章.md
```

**步骤3：清理残留的分隔符**
```bash
sed -i '/^---$/d' 正文/卷一_XXX/第X章.md
```

**步骤4：验证**
```bash
wc -l 正文/卷一_XXX/第X章.md
tail -3 正文/卷一_XXX/第X章.md
```

**绝对禁止**：用 `tail` 来裁剪文件——它会保留文件末尾而删除开头，是反向操作。

---

## 批量更新技巧

写完多章后一次性更新所有追踪文件：

1. 先写完所有章节（6-10章）
2. 确认每章文件都干净（末尾是正文，无审核报告残留）
3. 读取所有追踪文件当前状态（一次 read_file 多个）
4. 批量 patch/write_file 所有追踪文件
5. 最终一次验证

---

*最后更新：2025-05-09（增加事故修复 + 批量更新技巧）*

#### 世界状态.md

```markdown
# 世界状态

> 当前时间线：陨星历 127 年春 | 最后更新：第3卷

## 人物位置
| 角色 | 当前位置 | 状态 | 备注 |
|------|---------|------|------|
| 主角 | 观星塔 | 活跃 | 第2卷末抵达 |
| 导师 | 观星塔地下三层 | 被封印 | 第3卷被囚 |

## 关键物品位置
| 物品 | 位置 | 状态 |
|------|------|------|
| 上古器物 | 观星塔地下 | 被反派控制 |
```

#### 章节大纲.md

```markdown
# 章节大纲

> 记录每章核心事件和字数，便于追溯 | 自动追加

## 基本参数
- 每章目标字数：[自定义字数] 字（实际写作允许±20%浮动）
- 章节总数：90 章（第1卷30章 / 第2卷30章 / 第3卷30章）

## 第1卷

### 第1章：陨星坠落
- 字数：3200
- 核心事件：陨石坠落，主角发现体内有碎片反应
- 埋下伏笔：F001（封印碎片反应）
- 世界状态变化：主角从家中移动到陨石坠落点

### 第2章：观星塔来客
- 字数：2950
- 核心事件：神秘导师到访，邀请主角前往观星塔
- 埋下伏笔：F002（导师提到父亲）
```

#### 编年史.md

```markdown
# 编年史

> 按故事内时间排序 | 每次写新章节后追加

## 时间线

| 时间点 | 事件 | 对应章节 |
|--------|------|---------|
| 陨星历 127年 春 | 陨石坠落事件 | 第1卷第1章 |
| 陨星历 127年 春 | 主角离开家乡 | 第1卷第2章 |
| 陨星历 127年 夏 | 抵达观星塔 | 第1卷第5章 |
```

#### 文风指南.md

```markdown
# 文风指南

> 统一全书的写作风格 | 首次创建后固定，写新章节前必须重读

## 基本信息
- **作品基调**：冷峻克制
- **目标平台**：起点
- **受众定位**：男性向，16-35岁

## 叙事语调
- 第三人称全知视角
- 冷峻克制，不过度渲染情感
- 短句为主，长句用于心理描写

## 句式特征
- 动作描写：简洁有力，动词优先
- 对话：口语化，不书面
- 心理描写：间接呈现，不直接写"他感到"

## 词汇偏好
- 禁用：突然、顿时、赫然
- 倾向：陡然、骤然、陡然间
- 场景词：昏暗、幽微、冷峭

## 禁止事项
- 不写主角光环笼罩下的无敌感
- 不写配角无脑倒贴
- 不在同一章内重复使用同一个比喻

## 平台禁忌（起点）
- 不涉及真实的政治人物/事件
- 不触碰敏感情节
- 避免官场/文革等题材红线
```

#### 写作规范.md

```markdown
# 写作规范

> 本书的硬性规则 | 违反会导致文风崩塌

## 基本参数
- **人称/视角**：第三人称全知
- **每章字数**：[自定义字数] 字（±20%浮动）
- **章节总数**：90 章
- **预计总字数**：[自定义] 万字（章节数×每章字数）
- **发布平台**：起点
- **作品基调**：冷峻克制

## 硬性规则
1. **POV**：严格第三人称，每章只跟一个角色视角
2. **章节长度**：每章 [自定义字数] 字（允许 ±20% 浮动，以项目设定值为准）
3. **视角切换**：章节内不切换POV，章末可以悬念切换
4. **时间跳跃**：章内不跳跃时间，章间可以跳跃
5. **对话规则**：对话使用「」或""，根据世界设定选择
6. **场景描写**：每章至少一个完整场景感官描写（视/听/嗅/触/味）
| 一次 write_file 多段内容 | 写入不完整 | 分开多次调用 |
| 写完章节后用 head -n 裁剪时行数算错 | 审核报告残留，或正文被截断 | 见下文「事故修复：当审核报告已混入文件」 |

---

## 事故修复：当审核报告已混入文件

如果在写入时不小心把审核报告写进了章节文件，用以下步骤清理：

**步骤1：定位审核报告起始行**
```bash
grep -n "第X章审核报告" 正文/卷一_XXX/第X章.md
```
输出示例：`177:## 第X章审核报告` → 审核报告从第177行开始

**步骤2：裁剪文件**
```bash
head -n 176 正文/卷一_XXX/第X章.md > /tmp/chX_clean.md
mv /tmp/chX_clean.md 正文/卷一_XXX/第X章.md
```

**步骤3：清理残留的分隔符**
```bash
sed -i '/^---$/d' 正文/卷一_XXX/第X章.md
```

**步骤4：验证**
```bash
wc -l 正文/卷一_XXX/第X章.md
tail -3 正文/卷一_XXX/第X章.md
```

**绝对禁止**：用 `tail` 来裁剪文件——它会保留文件末尾而删除开头，是反向操作。

---

## 批量更新技巧

写完多章后一次性更新所有追踪文件：

1. 先写完所有章节（6-10章）
2. 确认每章文件都干净（末尾是正文，无审核报告残留）
3. 读取所有追踪文件当前状态（一次 read_file 多个）
4. 批量 patch/write_file 所有追踪文件
5. 最终一次验证

---

*最后更新：2025-05-09（增加事故修复 + 批量更新技巧）*

1. 每个模块必须写满，不能留空或敷衍一句
2. 所有内容基于剧情梗概合理推演，信息不足时自行补全
3. 正文（`正文/`目录）在首次生成时仅创建目录，不写内容；用户明确要求续写正文时再生成
4. 存放在 `~/hermes/novels/` 目录下
5. **每次写新章节前必须执行写前审查，完成后必须执行写后更新**